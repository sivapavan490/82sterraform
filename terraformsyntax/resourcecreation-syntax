resource "type-of-resource"   "name-of-resource"{
            (name is fixed)         (any name)

    required paarameters
}

ex:-

# output syntax:-
  output "name"{
    value= type_of_resource.name_of_resource
  }
# INSTANCE CREATION RESOURCE EXAMPLE
resource "aws_instance" "web" {
  ami           = "resolve:ssm:/aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64"
  instance_type = "t3.micro"

  tags = {
    Name = "HelloWorld"
  }
}


# SECURITY GROUP CREATION RESOURCE EXAMPLE
resource "aws_security_group" "securitygroup_creation" {
  name        = "siva" # security group name
  description = "Allow TLS inbound traffic and all outbound traffic"
    ingress {
    from_port        = 22
    to_port          = 22
    protocol         = "tcp"
    cidr_blocks      = ["0.0.0.0/0"]
    
  }
  egress {
    from_port        = 0
    to_port          = 0
    protocol         = "-1"
    cidr_blocks      = ["0.0.0.0/0"]
    
  }
  tags = {
    Name = "pavan" # name
    
  }
}

# refer 1 resource into another resource syntax in below:-

        type_of_resource.name_of_resource.value

        ex:- aws_security_group.securitygroup_creation.id


# variable syntax

variable "variable name"{
    "required parameters"
}

# condition syntax

  expression ? " this runs if true " : " this runs if false"
    # ex:- var.environment == "prod" ? "t2.micro" :"t3.small" 



# loop syntax
  They are 3 types of loops are avilable.they are

       1) count-based loop
       2) for -each loop
       3) dynamic cidr_blocks


count-based loop:-

* mainly count based loops are used to create multiple single resources at a time. count based loop helps to reduce the code.
Example for count-based lopp and count index visit the blow github link

```
https://github.com/sivapavan490/82sterraform/tree/ad438d284aa70a6013b4366db8c9e906068b875c/loops/count-loop
```

# data source -syntax

  data "type-of-resource" "name-of-resource"{
     
  }